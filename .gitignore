# Release artifacts go through the GitHub Releases asset uploader,
# never the regular git history — git push blocks anything over
# 100 MB and the installer comfortably exceeds that. Keeping them
# ignored at the repo root prevents the accidental `git add .` from
# poisoning the tree.
*.exe
*.msi
*.dmg
*.pkg
*.AppImage
*.zip
*.tar.gz
